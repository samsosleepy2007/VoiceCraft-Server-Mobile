name: Build Endstone Plugin

on:
  workflow_dispatch:
  push:
    branches: [main, endstone-phase1]
    paths:
      - "VoiceCraft.Endstone/**"
      - ".github/workflows/build-endstone.yml"

jobs:
  build-wheel:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install build and Endstone API
        run: |
          python -m pip install --upgrade pip
          python -m pip install build "endstone==0.11.10"

      - name: Compile plugin source
        run: python -m compileall -q VoiceCraft.Endstone/src

      - name: Build wheel
        working-directory: VoiceCraft.Endstone
        run: python -m build --wheel

      - name: Validate wheel contents and entrypoint
        working-directory: VoiceCraft.Endstone
        shell: bash
        run: |
          python - <<'PY'
          import glob
          import zipfile

          wheel = glob.glob("dist/*.whl")[0]
          with zipfile.ZipFile(wheel) as zf:
              names = set(zf.namelist())
              assert "endstone_voicecraft/config.toml" in names, names
              entry = [name for name in names if name.endswith(".dist-info/entry_points.txt")]
              assert entry, names
              text = zf.read(entry[0]).decode("utf-8")
              assert "voicecraft = endstone_voicecraft:VoiceCraftEndstone" in text, text
          print(f"Validated {wheel}")
          PY
          python -m pip install --no-deps dist/*.whl
          python - <<'PY'
          from endstone_voicecraft import VoiceCraftEndstone
          assert VoiceCraftEndstone.api_version == "0.11"
          assert VoiceCraftEndstone.version == "0.1.0"
          print("Endstone plugin import OK")
          PY

      - name: Upload Endstone wheel
        uses: actions/upload-artifact@v4
        with:
          name: VoiceCraft-Endstone-Phase1
          if-no-files-found: error
          path: VoiceCraft.Endstone/dist/*.whl
