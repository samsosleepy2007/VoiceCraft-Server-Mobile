<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools"
          android:installLocation="auto">
  <uses-permission android:name="android.permission.INTERNET" />
  <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
  <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
  <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
  <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
  <uses-permission android:name="android.permission.WAKE_LOCK" />
  <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

  <application
    android:label="VoiceCraft Server"
    android:icon="@drawable/voicecraft_logo"
    android:roundIcon="@drawable/voicecraft_logo"
    android:allowBackup="false"
    android:supportsRtl="true"
    android:usesCleartextTraffic="true">
    <!-- MainActivity is kept compiled as the UI3 fallback/reference, but it is
         disabled so Android exposes only the new ModernMainActivity launcher. -->
    <activity
      android:name="chat.voicecraft.server.MainActivity"
      android:enabled="false"
      tools:replace="android:enabled" />

    <service
      android:name="chat.voicecraft.server.VoiceCraftServerService"
      android:exported="false"
      android:foregroundServiceType="specialUse">
      <property
        android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
        android:value="User-started Minecraft proximity voice server" />
    </service>
  </application>
</manifest>
